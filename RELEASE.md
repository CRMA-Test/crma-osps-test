# Release process

Maintainers create releases from reviewed `main` commits after CI passes. Every release receives
a unique semantic-version tag. Release assets include that version in their filename and are
listed in the release notes with their SHA-256 digest.
