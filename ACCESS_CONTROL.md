# Access control

An organization owner assigns collaborator permissions manually after a documented maintenance
need. New collaborators receive no repository access through the organization base permission.
Owners review access when responsibilities change and remove access when it is no longer needed.

The `main` branch accepts changes through reviewed pull requests after required CI succeeds.
Repository and organization settings are the authority for enforcement details.
